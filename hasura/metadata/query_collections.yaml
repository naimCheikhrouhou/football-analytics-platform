- name: allowed-queries
  definition:
    queries:
      - name: GetUserProfile
        query: |
          query GetUserProfile {
            user_profiles {
              id
              role
              full_name
            }
          }

